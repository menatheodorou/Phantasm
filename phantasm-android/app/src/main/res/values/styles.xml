<resources>


    <!-- Base application theme. -->
    <style name="AppFullScreenTheme" parent="@style/Theme.AppCompat.Light.NoActionBar">

        <item name="colorPrimary">@color/navbar</item>
        <item name="colorPrimaryDark">@color/navbar</item>
        <item name="colorAccent">@color/navbar</item>

        <item name="android:navigationBarColor">@color/navbar</item>
        <item name="android:activatedBackgroundIndicator">@color/navbar</item>

        <item name="android:autoCompleteTextViewStyle">@style/cursorColor</item>

        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="AppTheme" parent="@style/Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/navbar</item>
        <item name="colorPrimaryDark">@color/navbar</item>
        <item name="colorAccent">@color/navbar</item>

        <item name="android:navigationBarColor">@color/navbar</item>
        <item name="android:activatedBackgroundIndicator">@color/navbar</item>

        <item name="android:autoCompleteTextViewStyle">@style/cursorColor</item>
    </style>

    <style name="AppToolbarTheme" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <!-- android:textColorPrimary is the  color of the title text
             in the Toolbar, in the Theme.AppCompat theme:  -->
        <item name="android:background">@color/black</item>
        <item name="android:textColorPrimary">@color/app_green_color</item>
    </style>

    <style name="cursorColor" parent="Widget.AppCompat.AutoCompleteTextView">
        <item name="android:textCursorDrawable">@drawable/ic_cursor</item>
    </style>

    <style name="tag">
        <item name="android:textColor">@color/text_color</item>
        <item name="android:textSize">15dp</item>
        <item name="android:background">@drawable/btn_tag</item>
    </style>
    <style name="FacebookLoginButton">
        <item name="android:textSize">@dimen/text_size</item>
        <item name="android:background">@color/blue_face_book</item>
        <item name="android:layout_marginTop">10dp</item>
        <item name="android:layout_marginBottom">10dp</item>
        <item name="android:layout_gravity">center_horizontal</item>
    </style>
</resources>